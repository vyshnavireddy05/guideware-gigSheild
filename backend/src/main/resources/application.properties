spring.datasource.url=${DATABASE_URL:jdbc:mysql://localhost:3306/gigshield}
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

app.jwt.secret=gigshield_secret_key_2026_secure
app.jwt.expiry=86400000

app.cors.allowed-origins=${CORS_ALLOWED_ORIGINS:http://localhost:5173}

weather.api.key=${WEATHER_API_KEY:YOUR_OPENWEATHERMAP_KEY_HERE}
weather.api.url=https://api.openweathermap.org/data/2.5

ml.service.url=${ML_SERVICE_URL:http://localhost:5000}

server.port=${PORT:8080}
